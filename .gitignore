# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Neural-TTS model weights (~337 MB) — fetch with pipeline/fetch_models.sh
pipeline/models/*.onnx
pipeline/models/*.bin

# Render outputs (regenerated by the pipeline)
**/out/
**/*.mp4
!pipeline/assets/particles.mp4
**/*.png
dist/
site/

# Source textbooks / copyrighted course inputs are never committed
*.pdf

# OS cruft
.DS_Store
